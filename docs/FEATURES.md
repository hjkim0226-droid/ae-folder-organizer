# AE Folder Organizer - 전체 기능 가이드

After Effects 프로젝트 패널 아이템 자동 정리 CEP 확장 프로그램의 모든 기능을 상세히 설명합니다.

---

## 목차

1. [폴더 자동 정리](#-1-폴더-자동-정리-core-feature)
2. [통합 필터 시스템](#-2-통합-필터-시스템)
3. [폴더 구조 커스터마이징](#-3-폴더-구조-커스터마이징)
4. [예외 규칙](#-4-예외-규칙-exception-rules)
5. [Batch Rename](#-5-batch-rename-일괄-이름-변경)
6. [라벨 컬러 시스템](#-6-라벨-컬러-시스템)
7. [프로젝트 통계](#-7-프로젝트-통계)
8. [🩺 Health Check](#-8-health-check-프로젝트-상태-점검)
9. [설정](#-9-설정)
10. [설정 관리](#-10-설정-관리)
11. [기본 폴더 구조](#-11-기본-폴더-구조)
12. [호스트 앱 지원](#-12-호스트-앱-지원)

---

## 📁 1. 폴더 자동 정리 (Core Feature)

### 카테고리 자동 분류

프로젝트 아이템을 5개 타입으로 자동 분류합니다:

| 카테고리 | 설명 |
|----------|------|
| **Comps** | 컴포지션 |
| **Footage** | 비디오 파일 |
| **Images** | 이미지 파일 |
| **Audio** | 오디오 파일 |
| **Solids** | 솔리드 및 널 오브젝트 |

### 렌더 컴프 자동 감지

다음 키워드가 포함된 컴포지션은 자동으로 렌더 폴더로 이동합니다:

- `_render`
- `_final`
- `_output`
- `_export`
- `RENDER_`
- `[RENDER]`

### 시퀀스 감지

`isStill` 속성을 활용하여 이미지 시퀀스를 자동 인식합니다.

### 확장자별 분류 기준

```
비디오 (Footage)
├── mp4, mov, avi, mkv, webm
├── m4v, wmv, flv, mxf

이미지 (Images)
├── jpg, jpeg, png, psd
├── tif, tiff, gif, bmp
├── ai, eps, svg

오디오 (Audio)
├── mp3, wav, aac, m4a
├── aif, aiff, ogg, flac

시퀀스 감지 대상
├── png, jpg, jpeg, tif, tiff, tga, bmp, gif
├── exr, dpx, cin, hdr
```

### 기타 기능

| 기능 | 설명 |
|------|------|
| **자동 넘버링** | 폴더 앞에 `00_`, `01_`, `99_` 형태의 번호 자동 부여 |
| **빈 폴더 삭제** | 정리 후 빈 폴더 자동 삭제 (옵션) |

---

## 🏷️ 2. 통합 필터 시스템

카테고리 및 서브카테고리에 적용 가능한 통합 필터 시스템입니다.

### 필터 타입

| 타입 | 문법 | 예시 | 태그 색상 |
|------|------|------|-----------|
| **확장자 필터** | `.확장자` | `.mp4`, `.mov` | 보라색 |
| **접두사 필터** | `prefix:접두사` | `prefix:VFX_`, `prefix:BG_` | 노란색 |
| **키워드 필터** | `키워드` | `fire`, `explosion` | 청록색 |

### 사용 예시

```
Footage 카테고리
├── 서브카테고리: Video → 필터: .mp4, .mov, .avi
├── 서브카테고리: VFX → 필터: prefix:VFX_
└── 서브카테고리: Fire Effects → 필터: fire, explosion
```

- 다중 필터 조합 지원
- 필터 우선순위: 먼저 매칭되는 서브카테고리로 이동

---

## 📂 3. 폴더 구조 커스터마이징

### 폴더 관리

| 기능 | 설명 |
|------|------|
| **무제한 폴더 생성** | 원하는 만큼 폴더 추가 가능 |
| **드래그 앤 드롭 정렬** | 카테고리/폴더 순서 변경 |
| **렌더 폴더 보호** | 삭제 불가, 최상위 고정 |
| **System 폴더** | Solids 전용, 99 번호로 하단 고정 |

### 서브카테고리

각 카테고리 내에 세부 폴더 구조를 만들 수 있습니다:

```
01_Source
├── 02_Footage
│   ├── 01_Video      ← 서브카테고리
│   ├── 02_VFX        ← 서브카테고리
│   └── 03_Others     ← 자동 생성 (매칭 안 된 항목)
```

### 서브카테고리 규칙

- 첫 번째 서브카테고리만 "All Items" 가능
- 두 번째부터는 필터 필수
- 매칭 안 된 항목은 자동으로 `Others` 폴더로 이동

---

## ⚡ 4. 예외 규칙 (Exception Rules)

특정 조건에 맞는 아이템을 지정된 폴더로 이동시킵니다.

### 예외 타입

| 타입 | 설명 | 예시 |
|------|------|------|
| **이름 포함** | 파일명에 특정 문자열 포함 시 | `_temp`, `backup` |
| **확장자** | 특정 확장자 일치 시 | `.fbx`, `.obj` |

### 사용 예시

```
예외 규칙 예시:
├── 이름에 "_temp" 포함 → System 폴더로 이동
├── 확장자 ".fbx" → Source/3D 폴더로 이동
└── 이름에 "backup" 포함 → Archive 폴더로 이동
```

예외 규칙은 다른 분류 규칙보다 **마지막에 적용**되어 최종 결정권을 가집니다.

---

## 🔤 5. Batch Rename (일괄 이름 변경)

선택한 아이템의 이름을 일괄 변경합니다.

### 기능 목록

| 기능 | 설명 |
|------|------|
| **선택 아이템 가져오기** | 프로젝트 패널에서 선택한 아이템 불러오기 |
| **폴더 재귀 처리** | 폴더 선택 시 내부 모든 아이템 포함 |
| **Prefix 추가** | 이름 앞에 문자열 추가 |
| **Suffix 추가** | 이름 뒤에 문자열 추가 |
| **Find & Replace** | 특정 문자열 찾아 바꾸기 |
| **실시간 미리보기** | 변경 전/후 이름 비교 (최대 10개 표시) |
| **Undo 지원** | `Ctrl+Z` / `Cmd+Z`로 되돌리기 가능 |

### 사용 예시

```
원본 이름: Scene01_v01.mov
├── Prefix: "VFX_" → VFX_Scene01_v01.mov
├── Suffix: "_final" → Scene01_v01_final.mov
└── Find: "v01" → Replace: "v02" → Scene01_v02.mov

조합 적용:
VFX_Scene01_v02_final.mov
```

---

## 🎨 6. 라벨 컬러 시스템

After Effects의 라벨 컬러(1-16)를 아이템에 자동 적용합니다.

### 적용 대상

| 대상 | 설명 |
|------|------|
| **폴더** | 폴더별 라벨 컬러 지정 |
| **카테고리** | 카테고리별 라벨 컬러 지정 |
| **서브카테고리** | 서브카테고리별 라벨 컬러 지정 |

### 우선순위

```
서브카테고리 라벨 > 카테고리 라벨 > 폴더 라벨
```

### 사용 가능한 라벨 컬러

| 번호 | 색상 | 번호 | 색상 |
|------|------|------|------|
| 1 | Red | 9 | Magenta |
| 2 | Yellow | 10 | Pink |
| 3 | Green-Yellow | 11 | Orange |
| 4 | Green | 12 | Brown |
| 5 | Cyan-Green | 13 | Teal |
| 6 | Cyan | 14 | Olive |
| 7 | Blue | 15 | Slate |
| 8 | Purple | 16 | Plum |

---

## 📊 7. 프로젝트 통계

현재 프로젝트의 아이템 통계를 표시합니다.

### 표시 항목

| 항목 | 설명 |
|------|------|
| **Comps** | 컴포지션 개수 |
| **Footage** | 비디오 파일 개수 |
| **Images** | 이미지 파일 개수 |
| **Audio** | 오디오 파일 개수 |
| **Sequences** | 이미지 시퀀스 개수 |
| **Solids** | 솔리드/널 오브젝트 개수 |
| **Folders** | 폴더 개수 |
| **Total Items** | 전체 아이템 개수 |

---

## 🩺 8. Health Check (프로젝트 상태 점검)

### 개요

프로젝트의 건강 상태를 점검하고 문제 아이템을 격리합니다.

### 지원 기능

| 기능 | 설명 | 대상 폴더 |
|------|------|----------|
| **Missing Footage 감지** | 연결 끊긴 푸티지 자동 감지 | `_Missing` |
| **Unused Asset 감지** | 렌더 컴프에서 사용되지 않는 아이템 감지 | `_Unused` |
| **Isolate Missing** | 누락 푸티지를 별도 폴더로 이동 | `_Missing` |
| **Isolate Unused** | 미사용 아이템을 별도 폴더로 이동 | `_Unused` |

### Unused Asset 감지 알고리즘

1. 렌더 키워드(`_render`, `_final`, `_output`)로 렌더 컴프 식별
2. 렌더 컴프 내 모든 레이어를 재귀적으로 스캔
3. 중첩 컴프의 레이어도 depth-first 방식으로 추적
4. 어떤 렌더 컴프에서도 사용되지 않는 아이템을 Unused로 분류
5. Solid/Null은 의도적 미사용이 많아 제외

### UI 표시

| 요소 | 설명 |
|------|------|
| **🔴 Missing: N** | Header에 누락 푸티지 개수 표시 |
| **🟡 Unused: N** | Header에 미사용 아이템 개수 표시 |
| **Isolate Missing 버튼** | 클릭 시 `_Missing` 폴더로 이동 |
| **Isolate Unused 버튼** | 클릭 시 `_Unused` 폴더로 이동 |

### Undo 지원

모든 격리 작업은 After Effects의 Undo 시스템과 통합됩니다.
- `Ctrl+Z` (Windows) / `Cmd+Z` (macOS)로 복원 가능
- Undo Group: "Isolate Missing Footage", "Isolate Unused Assets"

---

## ⚙️ 9. 설정

### 사용 가능한 설정

| 설정 | 설명 | 기본값 |
|------|------|--------|
| **Show source overview** | 헤더에 통계 표시 | ✅ On |
| **Delete empty folders** | 정리 후 빈 폴더 자동 삭제 | ✅ On |
| **Apply label color to folders** | 폴더 자체에도 라벨 적용 | ❌ Off |

---

## 💾 10. 설정 관리

### 기능 목록

| 기능 | 설명 |
|------|------|
| **Export Config** | JSON 파일로 설정 내보내기 |
| **Import Config** | JSON 파일에서 설정 가져오기 |
| **Reset to Default** | 기본 설정으로 초기화 |
| **자동 저장** | localStorage에 자동 저장 |
| **버전 마이그레이션** | 구버전 설정 자동 업그레이드 |

### 설정 파일 형식

```json
{
  "version": 5,
  "folders": [
    {
      "id": "render",
      "name": "Render",
      "order": 0,
      "isRenderFolder": true,
      "renderKeywords": ["Render"],
      "categories": []
    }
  ],
  "exceptions": [],
  "settings": {
    "deleteEmptyFolders": true,
    "showStats": true,
    "applyFolderLabelColor": false
  }
}
```

---

## 🎯 11. 기본 폴더 구조

정리 후 생성되는 기본 폴더 구조입니다:

```
프로젝트 패널
├── 📁 00_Render                ← 렌더 컴프 자동 이동
│   └── MainComp_render_v01
│
├── 📁 01_Source
│   ├── 01_Comps/
│   ├── 02_Footage/
│   │   ├── Sequences/
│   │   │   └── EXR Sequence/
│   │   └── _MP4/
│   ├── 03_Images/
│   └── 04_Audio/
│
└── 📁 99_System
    └── 01_Solids/
```

### 폴더 번호 규칙

| 번호 | 용도 |
|------|------|
| `00_` | 렌더 폴더 (고정) |
| `01_` ~ `98_` | 사용자 폴더 |
| `99_` | 시스템 폴더 (고정) |

---

## 🖥️ 12. 호스트 앱 지원

### 지원 현황

| 앱 | 상태 | 특이사항 |
|----|------|----------|
| **After Effects** | ✅ 완전 지원 | 주 타겟 |
| **Premiere Pro** | ⚠️ 실험적 | 용어 자동 변환 |

### Premiere Pro 용어 변환

| After Effects | Premiere Pro |
|---------------|--------------|
| Folder | Bin |
| Comp | Sequence |
| Footage | Clip |

---

## 기술 사양

### 아키텍처

```
CEP Extension
├── UI Layer (React 19 + TypeScript)
├── Domain Layer (Pure TypeScript)
└── ExtendScript (ES3 호환)
```

### 지원 버전

- After Effects 2023+
- Premiere Pro 2023+ (실험적)

### 개발 프레임워크

- [Bolt CEP](https://github.com/hyperbrew/bolt-cep)
- React 19
- TypeScript
- Vite
